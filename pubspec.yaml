name: des_engine
description:  A des decryption engine, he only has dart code
version: 0.0.1
repository: https://github.com/Wayaer/des_engine

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.1